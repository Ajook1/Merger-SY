{
  "version": 2,
  "builds": [
    {
      "src": "requirements.txt",
      "use": "@vercel/python"
    },
    {
      "src": "app.py",
      "use": "@vercel/python",
      "env": {
        "FLASK_APP": "app"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app"
    }
  ]
}
